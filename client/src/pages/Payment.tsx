import { Box, Stack, Typography } from "@mui/material";

export default function Payment() {
  return (
    <Stack spacing={3}>
      <Typography id="title">Payment</Typography>

      <Box>
        <Typography id="subtitle">Payment</Typography>
      </Box>
    </Stack>
  );
}

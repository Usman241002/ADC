import { Box, Stack, Typography } from "@mui/material";

export default function Vehicles() {
  return (
    <Stack spacing={3}>
      <Typography id="title">Vehicles</Typography>

      <Box>
        <Typography id="subtitle">Vehicles</Typography>
      </Box>
    </Stack>
  );
}
